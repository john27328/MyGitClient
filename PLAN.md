# План MyGitClient

Это живой чек-лист проекта. После завершения задачи нужно отметить пункт и при
необходимости добавить краткое пояснение или новый пункт. Этап считается завершённым,
только если код проверен Ruff, Pyright и pytest.

## 0. Основа проекта

- [x] Создать устанавливаемый Python-проект с `pyproject.toml`.
- [x] Добавить PySide6 и главное окно на Qt Widgets.
- [x] Добавить системную, светлую и тёмную темы.
- [x] Сохранять геометрию окна и выбранную тему.
- [x] Исправить переключение темы без перезапуска: полностью обновлять palette/style у
  diff, gutter, Side-by-side и остальных дочерних панелей при переходах
  System/Light/Dark в обе стороны; покрыть регрессию UI-тестом.
- [x] Настраивать и сохранять размеры шрифта интерфейса и diff.
- [x] Настроить файловое логирование с ротацией.
- [x] Настроить Ruff, строгий Pyright, pytest и pytest-qt.
- [x] Добавить CI для Windows, macOS и Linux; автоматический push-запуск оставить только
  для `master`, а `develop` проверять вручную, через PR или release workflow.
- [ ] Вернуть macOS в CI после стабилизации асинхронного amend UI-теста; временно
  проверять только Windows и Ubuntu.
- [x] Добавить иконку приложения и базовые ресурсы Qt.

## 1. Git-ядро

- [x] Находить системный исполняемый файл `git`.
- [x] Запускать Git-команды неблокирующе через `QProcess`.
- [x] Поддержать stdout, stderr, завершение и отмену команды.
- [x] Добавить типизированные модели команды и результата.
- [x] Разбирать `git status --porcelain=v2 --branch -z`.
- [x] Разбирать staged, unstaged, untracked, ignored, rename и conflict.
- [x] Разбирать branch, upstream и ahead/behind.
- [x] Проверять runner на настоящем временном репозитории.
- [x] Добавить централизованную очередь изменяющих Git-процессов; чтения выполнять параллельно.
- [x] Показывать активную Git-операцию и очередь команд с их репозиториями.
- [x] Показывать очередь в одном меню и позволять отменить каждую активную или ожидающую операцию.
- [x] Добавить структурированные категории ошибок Git.
- [x] Понятно объяснять блокировку `index.lock` и безопасное восстановление после зависшего Git-процесса.
- [ ] Добавить журнал выполненных команд без секретов и чувствительных данных.

## Технический рефакторинг

- [x] Освобождать завершённые `GitRunner` и принадлежащие им `QProcess`.
- [x] Отсекать устаревшие status, diff и history-ответы поколениями запросов.
- [x] Передавать репозиторий вместе с результатом diff.
- [x] Обрабатывать пользовательскую отмену отдельно от ошибки Git.
- [x] Вынести состояние выбора строк и hunk из `MainWindow`.
- [x] Вынести таблицу истории, пагинацию и построение графа в `HistoryPanel`.
- [x] Вынести редакторы, gutter, toolbar и splitter представлений diff в `DiffView`.
- [x] Вынести рендеринг unified/side-by-side, selection и scroll restore в `DiffView`.
- [x] Передать `DiffView` владение выбором строк/hunk и сигналами частичного stage.
- [x] Вынести дерево изменений и commit-форму в `ChangesPanel`.
- [x] Вынести список, переключатель и действия репозиториев в `RepositoriesPanel`.
- [x] Вынести diff, changes, commit и repositories в отдельные QWidget-панели.
- [x] Разделить UI-тесты по функциональным областям.
- [x] Выполнять поиск вложенных репозиториев вне GUI-потока.
- [x] Игнорировать ранние Qt-сигналы вкладок до завершения создания всех панелей окна.

## 2. Рабочее пространство

- [x] Добавить `AppShell` с постоянной стартовой Home-вкладкой без активного Git-контекста:
  показывать Open local repository, recent repositories и named workspaces; каждый репозиторий
  открывать в отдельной закрываемой вкладке.
  - [x] Добавить самостоятельный `HomePanel` и запускать приложение без автоматической
    активации последнего репозитория.
- [x] Вынести состояние репозитория в изолированный `RepositorySession`: каждая вкладка владеет
  собственными `GitService`, очередью, таймерами, selection и поколением запросов; переключение
  вкладки не должно переиспользовать панели или принимать ответы другой вкладки.
- [x] Убрать горячую замену репозитория внутри живой repository session; открытие recent,
  workspace, nested repository и submodule должно активировать существующую вкладку этого пути
  либо создавать новую изолированную вкладку.
- [x] Открывать репозиторий через диалог выбора папки.
- [x] Находить корень репозитория из вложенной директории.
- [x] Хранить и отображать недавние репозитории.
- [x] Перенести недавние репозитории из боковой панели во всплывающее меню toolbar;
  открытые репозитории переключать отдельным верхним списком.
- [x] Объединить recent и переключатель открытых репозиториев в одно древовидное меню toolbar.
- [x] Показывать дерево репозиториев в одном popup без вложенных подменю и отображать Pull/Push-статус каждого открытого репозитория.
- [x] Загружать и показывать ветку и список изменённых файлов.
- [x] Обнаруживать внешний checkout/изменение `HEAD` и автоматически обновлять заголовок окна,
  историю, refs, status и diff без перезапуска приложения; ручной Refresh обязан читать текущее
  состояние Git и не оставлять имя прежней ветки.
- [x] Пересмотреть назначение кнопки Refresh после надёжного автообновления: убрать её из
  основного toolbar либо оставить как явное аварийное полное перечитывание репозитория с
  понятным tooltip, а не как обязательную часть обычного рабочего цикла.
- [x] Добавить удаление проекта из списка недавних через контекстное меню.
- [x] Восстанавливать последний открытый репозиторий при запуске.
- [x] Поддержать несколько одновременно открытых репозиториев.
- [x] Стабилизировать переключение репозиториев: останавливать автоопрос на время
  сброса UI и запускать чтение состояния только для последней активации.
- [x] Добавить именованные workspace с набором репозиториев.
- [x] Обнаруживать вложенные репозитории, submodules и worktrees.

## 3. Основной рабочий цикл — v0.1

- [x] Показывать unified diff выбранного файла.
- [x] Поддержать diff для staged и unstaged версии.
- [x] Показывать источник diff обычной подписью, если вариант один; выбор показывать только для частично staged-файла.
- [x] Добавить переключение представления diff: Unified и Side-by-side.
- [x] Сохранять выбранное представление diff в настройках приложения.
- [x] Добавить номера строк старой и новой версии.
- [x] Подсвечивать заголовки, hunks, добавленные и удалённые строки с учётом темы.
- [x] Сопоставлять удалённые и добавленные строки для двухколоночного представления.
- [x] Синхронизировать вертикальную прокрутку колонок в режиме Side-by-side.
- [x] Добавить внутристочную подсветку изменённых фрагментов.
- [x] Добавить подсветку ключевых слов, строк, комментариев и чисел для Python, C/C++, C#,
  Java/Kotlin, JavaScript/TypeScript, JSON, YAML, shell и Markdown.
- [x] Корректно показывать пустые, бинарные и слишком большие diff.
- [x] Добавить кнопку-переключатель переноса строк в toolbar diff.
- [x] Сохранять настройку переноса строк и применять её в Unified и Side-by-side.
- [x] Добавить настройку отображения пробельных символов.
- [x] Добавить сохраняемую опцию diff для игнорирования изменений пробелов.
- [x] Добавить режимы контекста diff: расширение hunk и отображение полного файла,
  а также мини-карту добавлений/удалений на полосе прокрутки.
- [x] Не применять внутристрочную подсветку к слабо похожим парам строк.
- [x] Настроить минимальные размеры панелей и сохранять положение разделителей.
- [x] Покрыть оба представления diff и цветовые роли UI-тестами.
- [x] Обновлять status и diff после изменения файлов на диске.
- [x] Очищать оба представления diff сразу при переключении репозитория.
- [x] Сохранять позицию прокрутки при фоновом обновлении неизменившегося diff.
- [x] Реализовать stage и unstage целого файла.
- [x] Добавить staging чекбоксами в списке файлов в стиле GitHub Desktop.
- [x] Изменить политику checkbox в Changes и diff: checkbox только формирует устойчивый
  выбор, а отдельные Stage/Stash/Unstage/Discard применяют действие; фактическое состояние
  Git показывать иконками и подписями, выбор файлов сохранять через refresh, а выбор строк
  diff хранить только для открытого файла; дублировать действия в toolbar diff и различать
  staged/unstaged/частичное состояние цветным badge поверх иконки типа изменения.
- [x] После отказа от немедленного staging вернуть единое правило выбора: checkbox файла,
  строки и hunk только меняет локальное выделение, а Git index изменяется одной пакетной
  операцией по кнопке Stage/Unstage; выделение показывать синим независимо от типа diff,
  фактическое состояние Git — отдельным синим/оранжевым маркером в дереве и gutter,
  а полностью индексированный файл — заметным badge `IN INDEX · WHOLE FILE` над diff.
- [x] Убрать отдельные колонки Index/Working tree из списка изменений: показывать тип
  изменения компактной иконкой, staging — checkbox, подробные состояния — в tooltip.
- [x] Добавить переключение плоского списка и дерева изменений с групповыми checkbox папок.
- [x] Схлопывать цепочки папок с единственным потомком в одну строку дерева Changes.
- [x] Ограничить схлопывание дерева Changes только цепочками каталогов: файл всегда
  показывать отдельным листом с собственным именем, не объединяя его с родительским путём.
- [x] Сохранять позицию прокрутки Changes при stage/unstage и фоновом обновлении status.
- [x] Стабильно сортировать Changes по полному пути, чтобы файлы не перемещались после stage/unstage.
- [x] Добавить общий чекбокс Stage/Unstage all с промежуточным состоянием.
- [x] Исправить групповой stage/unstage через checkbox папки в дереве Changes: собирать
  все дочерние файлы в одну пакетную Git-операцию и один запрос обновления, не запускать
  конкурирующие команды для каждого файла и не провоцировать конфликт `index.lock`;
  покрыть stage и unstage корневых и вложенных папок интеграционными UI-тестами.
- [x] Показывать частично staged-файл промежуточным состоянием чекбокса.
- [x] Добавить сохраняемый режим Changes `Combined` / `Split`; в Split показывать
  независимые деревья Unstaged и Staged, а diff выбирать по дереву в фокусе.
- [x] Не дублировать Untracked в колонке Index: показывать его только в Working tree.
- [x] Реализовать stage и unstage выбранного hunk.
- [x] Добавить немедленный line stage/unstage: клик по строке в gutter сразу применяет patch.
- [x] Поддержать checkbox-выбор строк в Unified и Side-by-side представлениях diff.
- [x] Показывать checkbox строк и hunk по фактическому состоянию index после обновления diff.
- [x] Применять stage/unstage hunk сразу по клику на checkbox его заголовка.
- [x] Поддержать немедленное применение диапазона строк мышью.
- [x] Формировать корректный частичный patch с пересчитанными диапазонами hunk.
- [x] Сохранять file-mode metadata и `/dev/null` в частичном patch нового файла, чтобы
  line/hunk stage работал для untracked-файлов.
- [x] Поддержать line unstage для staged diff тем же интерфейсом.
- [x] Убрать pending-выбор и отдельные кнопки применения/сброса: источником состояния является Git index.
- [x] При выборе stage целого файла визуально выделять все изменённые строки и hunk в diff,
  а при unstage снимать это выделение.
- [x] Покрыть line stage/unstage интеграционными тестами на реальном Git-репозитории.
- [ ] Удалить оставшийся скрытый механизм немедленного применения selection из `DiffView`:
  `selected_lines_button`, `clear_lines_button`, старые `lines_requested`/`hunk_requested` и
  связанные тесты; checkbox должен только выделять, а Git изменяют явные кнопки действий.
- [ ] Устранить противоречие со старой политикой немедленного line/hunk stage в плане и
  зафиксировать единую актуальную семантику checkbox для дерева Changes и diff.
- [ ] Добавить сквозной UI-тест частичного stage untracked-файла через выделение строк и
  кнопку Stage.
- [ ] Добавить интеграционные тесты корректного частичного patch для удалённого и
  переименованного файла, включая file-mode/rename metadata и `/dev/null`.
- [x] Исправить формирование patch при выборе нескольких несмежных строк: не допускать
  `corrupt patch`, корректно разбивать выбор по hunk, пересчитывать диапазоны и покрыть сценарий
  интеграционным тестом с реальным `git apply --cached`.
- [x] После успешного Stage/Stash/Unstage/Discard снимать checkbox-выбор обработанных файлов,
  папок и строк diff; при ошибке сохранять неприменённую часть выбора, чтобы действие можно было
  повторить и пользователь не терял контекст.
- [x] Исправить Discard нескольких выбранных файлов: показывать одно подтверждение, выполнять
  tracked/untracked изменения согласованной пакетной операцией без параллельных Git-команд,
  `index.lock` и каскада одинаковых ошибок; покрыть смешанный набор файлов UI-интеграционным тестом.
- [x] Исправить вертикальное смещение checkbox и номеров строк относительно содержимого diff
  на больших файлах: gutter должен использовать геометрию тех же визуальных строк при скрытых
  служебных блоках, изменении размера шрифта, переносе строк и переключении темы.
- [x] Повысить контраст diff в светлой/System теме: основной код, синтаксические токены,
  hunk-заголовки и номера строк должны оставаться читаемыми на мягких фонах изменений.
- [ ] Исследовать поблочный рендеринг diff: один общий источник layout для текста, номеров строк,
  checkbox и hunk вместо синхронизации независимых `QPlainTextEdit`; выбрать миграцию, если она
  надёжнее устраняет рассинхронизацию gutter в Unified и Side-by-side.
- [x] Добавить поле сообщения и создание commit.
- [x] Добавить description коммита и автозаполнение по staged-файлам.
- [x] Сворачивать автосгенерированный description больших commit по папкам.
- [x] Проверять пустое сообщение и отсутствие staged-изменений.
- [x] Добавить amend последнего commit.
- [x] В amend корректно отмечать файлы последнего commit с пробелами и Unicode в пути; очищать preview при выходе из amend.
- [x] При включении amend показывать сообщение, description и полный diff последнего commit.
- [x] Сделать amend-workspace: добавлять и исключать файлы с сохранением изменений локально.
- [x] Исправить визуальное выделение файлов и состояний checkbox в amend-workspace.
- [x] Добавить безопасный discard с подтверждением.
- [x] Добавить открытие выбранного изменённого файла в системном файловом менеджере
  через контекстное меню.
- [x] Поддержать discard и stash одного или нескольких выбранных файлов.
- [x] Поддержать checkout с временным stash и восстановлением локальных изменений.
- [x] Добавить базовое управление `.gitignore`.
- [x] Показывать прогресс, ошибки и возможность отмены всех операций.

## 4. История и ветки — v0.2

- [x] Добавить лениво загружаемый список коммитов порциями по 100 записей.
- [x] Дорисовать граф связей коммитов с ветвлениями и merge-линиями.
- [x] Показывать сведения, изменённые файлы и diff выбранного commit.
- [x] Перестроить History в три визуальные колонки `Refs | History + commit/files | Diff`:
  сведения и файлы commit располагать под историей, refs делать скрываемыми, добавить
  сохраняемый режим фокуса на diff и сохранять размеры обеих пар панелей.
- [x] Добавить быстрое закрытие diff файла из истории: кнопка-крестик и `Esc`, с
  возвратом фокуса к списку файлов/истории без сброса выбранного commit.
- [x] Показывать локальные и remote-ветки с upstream и ahead/behind.
- [x] Реализовать checkout и создание ветки.
- [x] Реализовать переименование, безопасное удаление и явное force-удаление ветки с подтверждением.
- [x] Добавить управление ветками через контекстное меню.
- [x] Дополнить контекстное меню копированием имени и подтверждаемым удалением remote-ветки
  через системный Git без удаления одноимённой локальной ветки.
- [x] Добавить в контекстное меню создание ветки от выбранного ref, публикацию локальной
  ветки с upstream и быстрое сравнение local-ветки с её upstream.
- [x] Показывать прямо в дереве refs текущую, неопубликованную и потерявшую upstream ветку,
  а также ahead/behind; пояснять состояние в tooltip и учитывать его в контекстных действиях.
- [x] Добавить быстрый фильтр загруженной истории по сообщению, автору, email и hash.
- [x] Всегда показывать автора отдельной колонкой в истории и позволять назначать автору
  сохраняемый персональный цвет через контекстное меню (идентификация по email).
- [x] Добавить просмотр, создание, удаление и публикацию tags.
- [x] Фильтровать историю по refs: по умолчанию показывать текущую ветку и позволять выбрать
  максимум одну дополнительную ветку для сравнения.
- [x] Добавить рядом с историей единое фильтруемое дерево refs: local branches, remotes,
  tags, stashes и submodules; выбор ref фильтрует историю, контекстное меню управляет ref.
- [x] Добавить первый срез дерева refs: local branches, remotes и tags, поиск, выбор ref
  для истории и контекстные операции веток/tags.
- [x] Дополнить дерево refs разделами Stashes и Submodules.
- [x] Добавить Apply/Pop/Drop для stash с подтверждением удаления и открытие submodule.
- [x] После переноса всех действий в дерево refs убрать отдельные вкладки Branches и Tags.
- [x] Добавить сравнение веток: показывать изменённые файлы и diff для выбранной пары refs.
- [x] Показывать в истории позиции local/remote веток и tags, а также точку ответвления
  текущей ветки от `main`/`master`.
- [x] Добавить очистку удалённых веток: `fetch --prune` для исчезнувших remote refs и
  безопасное массовое удаление локальных веток с пометкой `upstream gone`, с выбором и
  явным подтверждением force-удаления неслитых веток.
- [x] Добавить безопасный cherry-pick выбранного commit и диапазона commits из истории:
  предварительный список commits/файлов, подтверждение, проверка грязного рабочего дерева
  и опциональный autostash.
- [x] Добавить revert выбранного commit и диапазона commits с предварительным просмотром
  и подтверждением создаваемых обратных commits.
- [x] Добавить обычный rebase текущей ветки на выбранную local/remote-ветку: показывать
  base и переносимые commits, запрещать неоднозначный запуск и поддержать autostash.
- [x] Добавить редактор interactive rebase: reorder и команды pick/reword/edit/squash/
  fixup/drop, валидация плана до запуска и безопасная передача todo системному Git без
  открытия внешнего редактора.
- [x] Перед переписыванием истории показывать точку восстановления и после успешной
  операции предлагать открыть reflog/скопировать предыдущий HEAD.

## 5. Синхронизация

- [x] Показывать remotes и tracking branch.
- [x] Реализовать fetch.
- [x] Добавить Fetch all для всех открытых репозиториев через общую очередь операций.
- [x] Реализовать pull с явным выбором merge/rebase и опциональным autostash.
- [x] Реализовать push и настройку upstream через origin.
- [x] Показывать ahead/behind.
- [x] Предупреждать о divergence, когда обычный push будет отклонён, и предлагать rebase или force-with-lease.
- [x] Показывать текущую сетевую Git-операцию, блокировать дубли и давать Cancel.
- [x] Показывать фазу и длительность долгой Git-операции в toolbar/status bar.
- [x] Показывать очищенный от credentials живой stdout/stderr для операций очереди.
- [ ] Поддержать SSH, HTTPS, credential helpers, hooks и Git LFS через системный Git.
- [x] Поддержать только безопасный `--force-with-lease` в основном UI.

## 6. Merge workspace — v0.3

- [x] Добавить предварительный просмотр merge.
- [x] Показывать входящие commits и изменяемые файлы.
- [x] Определять незавершённые merge/rebase/cherry-pick/revert при обновлении status и
  восстанавливать панель операции после перезапуска клиента.
- [x] Реализовать Continue, Skip и Abort с подтверждением отмены и учётом допустимых
  действий для каждого типа операции.
- [x] Показывать тип, текущий шаг и очередь commits для незавершённых cherry-pick/rebase;
  после перезапуска приложения восстанавливать этот экран из состояния системного Git.
- [x] Показывать список конфликтующих файлов.
- [x] Добавить однооконный редактор итогового файла с навигацией по конфликтным
  блокам, ручным редактированием и сохранением результата в index.
- [x] Поддержать выбор current/incoming/both для отдельного конфликтного блока.
- [x] Добавить переключаемое сравнение current/incoming для выбранного блока.
- [x] Дополнить подробное сравнение вариантом base и полноценным diff каждой стороны
  относительно общего предка.
- [x] Проверять оставшиеся conflict markers.
- [x] Поддержать запуск внешнего merge tool.
- [x] Добавить отдельное разрешение бинарных конфликтов с учётом `.gitattributes`:
  показывать превью изображений или метаданные/хеш остальных файлов и позволять выбрать
  current, incoming либо удаление результата без декодирования бинарных данных как текста.
- [x] Расширить встроенные превью бинарных конфликтов без дополнительных зависимостей:
  изображения/SVG/GIF через Qt, первую страницу PDF через QtPdf, содержимое ZIP/Office через
  стандартный `zipfile` и образец шрифта через Qt; для неподдержанных системным Qt медиа
  оставлять безопасную карточку формата, размера и SHA-256.

## 7. Submodules и worktrees — v0.4

- [x] Показывать иерархию submodules, включая рекурсивно вложенные submodules.
- [x] Помечать submodule в Changes, показывать его Push/Pull статус и открывать двойным кликом.
- [x] Показывать ожидаемый и фактически checkout-нутый commit submodule.
- [x] Реализовать init, update и sync через контекстное меню submodule в Changes.
- [x] Добавить рекурсивные init, update и sync с явным подтверждением.
- [x] Добавить сохраняемые опции Include submodules для Fetch, Pull и Push текущего репозитория.
- [ ] Показывать и создавать worktrees.
- [ ] Открывать submodule/worktree как самостоятельный репозиторий.

## 8. GitHub — v0.5

- [ ] Добавить на Home профили нескольких GitHub-аккаунтов и явную привязку локального
  репозитория к API-аккаунту, clone transport и локальным `user.name`/`user.email`.
- [ ] Реализовать OAuth Device Flow как основной вход (с возможностью подтвердить код на другом
  устройстве), fine-grained PAT как ручной fallback и позднее необязательный импорт аккаунтов из
  GitHub CLI; секреты хранить только в системном credential store, не в `QSettings`.
- [ ] Добавить браузер репозиториев выбранного GitHub-аккаунта с фильтром owner/organization,
  поиском, выбором HTTPS/SSH, целевой директории и асинхронным clone через системный Git.
- [ ] Разделять GitHub API authorization и Git transport credentials: не передавать API-токены в
  аргументах Git, уважать системные credential helpers/SSH и предупреждать о несовпадении
  выбранного API-аккаунта с настроенной commit/push identity.
- [ ] Реализовать OAuth Device Flow.
- [ ] Хранить токен в системном keyring.
- [ ] Определять GitHub-репозиторий по remote.
- [ ] Показывать список и детали pull requests.
- [ ] Показывать файлы, reviews и CI checks.
- [ ] Поддержать checkout ветки pull request.
- [ ] Создавать и редактировать pull request.
- [ ] Поддержать merge/squash/rebase pull request.
- [ ] Поддержать комментарии и запрос reviewers.
- [ ] Сохранять полноценную локальную работу без GitHub-авторизации.

## 9. Поставка

- [x] Разделить рабочую ветку `develop` и релизную ветку `master`.
- [x] Добавить локальную portable-сборку Windows в ZIP со встроенным Python runtime и ярлыком запуска.
- [ ] Собрать приложение PyInstaller для трёх платформ.
- [x] Добавить smoke-тест упакованного приложения после распаковки portable ZIP.
- [x] Публиковать portable ZIP и SHA-256 через GitHub Actions для тегов и ручных запусков.
- [x] Добавить автоматические GitHub Releases из version-тегов релизной ветки.
- [x] Автоматизировать полный CI/CD-релиз одной командой или `workflow_dispatch`: принять
  номер версии, проверить чистый и синхронизированный `develop`, дождаться CI, обновить
  version-файлы, fast-forward `master`, создать и отправить tag, собрать artifacts и
  опубликовать GitHub Release; предусмотреть dry-run, защиту от повторного тега и понятный
  итоговый отчёт без ручного опроса каждого workflow.
- [x] Упростить ручной запуск Release workflow: разрешить пустую версию и автоматически
  повышать patch относительно текущей версии; явный номер оставить для minor/major и
  нестандартных релизов.
- [ ] Добавить экспорт диагностического отчёта без секретов.
- [x] Добавить автоматическую и ручную проверку новых GitHub Releases.
- [x] Добавить обновление Windows portable в один клик: скачать ZIP, проверить SHA-256,
  заменить файлы внешним updater и перезапустить приложение.
- [ ] Подписывать публичные сборки Windows и macOS.

## Ближайший срез

- [x] Выбор файла в списке изменений.
- [x] Асинхронная команда получения diff.
- [x] Панель просмотра unified diff.
- [x] Тесты diff-парсера и полного UI-сценария.

## Следующий срез — доработка diff

- [x] Расширить модель diff номерами старых и новых строк и структурой hunks.
- [x] Заменить текстовую панель на модель/представление с цветовой подсветкой diff.
- [x] Добавить переключатель Unified / Side-by-side и сохранить выбор в `QSettings`.
- [x] Проверить адаптивные размеры панелей на обычном и широком окне.
- [x] Реализовать синхронную прокрутку и выравнивание строк в Side-by-side.
- [x] Упростить чтение diff: вынести путь в единый заголовок, скрыть служебные строки Git,
  визуально разделить hunks и стабилизировать выравнивание крупных изменённых блоков.
- [x] Добавить UI-тесты обоих режимов и переключения темы.

## Уточнения реализованного поведения

- [x] Применять смену темы через перезапуск приложения и удалить устаревшие пути
  динамического обновления темы.
- [x] Если галочками ничего не выбрано, применять Stage/Stash/Unstage/Discard к текущей
  строке файла; явный выбор галочками имеет приоритет.
